import styled from 'styled-components';

const StyledDetailsContainer = styled.ul`
  display: grid;
  gap: 0.8rem;
  list-style: none;
  margin: 0;
  padding: 0;
`;

export default StyledDetailsContainer;
